[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "diffusionfusion"
version = "0.0.0"
description = "Local packaging for DiffusionFusion modules."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["custom*"]
